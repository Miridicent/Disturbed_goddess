/// @description Insert description here
// You can write your code in this editor

// Increment the message index
message_index += 1;

// Loop back to the first message if we've gone past the last one
//if (message_index >= array_length_1d(messages)) {
   // message_index = 0;
//}

// Update the textbox text
text = messages[message_index]; // Update the textbox text